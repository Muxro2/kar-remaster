import { Aubrey as Primary } from 'next/font/google'
import { Ephesis as Secondary } from 'next/font/google'

export const font_primary = Primary({
   weight: ['400'],
   subsets: ['latin'],
  })

  export const font_seconday = Secondary({
   weight: ['400'],
   subsets: ['latin'],
  })